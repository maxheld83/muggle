options(
  repos = c(
    RSPM = paste0(
      "https://packagemanager.rstudio.com/all/__linux__/focal/",
      # this freezes the dependencies, see https://github.com/subugoe/muggle/issues/60
      c(`2020-08-10` = 309)
    )
  )
)
